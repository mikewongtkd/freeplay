'use strict';
const {result}=require('./test-utils');
module.exports=[{id:'load.capacity',category:'Load / Capacity',name:'42-Stream Capacity',description:'Explicit observational capacity test; never starts automatically.',severity:'high',testType:'load-test',locks:['server:load'],requiresTestMode:true,evaluate(ctx,o,obs){const target=Number(o.targetStreams||42),active=ctx.liveStreams.size,mem=process.memoryUsage(),protocolErrors=ctx.metrics.protocolErrors;const state=active>=target&&protocolErrors===0?'PASS':active? 'WARN':'FAIL';return result(this,{status:state,expected:{activeStreams:target,protocolErrors:0},actual:{activeStreams:active,protocolErrors},metrics:{rssBytes:mem.rss,heapUsedBytes:mem.heapUsed,uptimeSeconds:process.uptime(),eventsObserved:obs.length},observations:['This test observes valid connected clients; it does not fabricate encoded H.264 payloads.'],recommendation:state==='PASS'?null:'Run the external FPV1 camera simulator or connect the target number of cameras.'});}}];
