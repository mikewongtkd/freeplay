'use strict';
const {result,stats}=require('./test-utils');
module.exports=[{id:'timing.pts',category:'Timing',name:'PTS Accuracy',description:'Measures PTS ordering and frame spacing without comparing tablet clocks across cameras.',severity:'critical',testType:'observational',locks:o=>[`stream:${o.streamId}`],evaluate(ctx,o,obs){const pts=obs.filter(x=>x.type==='binary').map(x=>BigInt(x.ptsUs)),d=[];let backward=0,duplicates=0,large=0;for(let i=1;i<pts.length;i++){const v=Number(pts[i]-pts[i-1]);d.push(v);if(v<0)backward++;if(v===0)duplicates++;if(v>100000)large++;}const m=stats(d),state=!d.length?'FAIL':backward?'FAIL':large||duplicates?'WARN':'PASS';return result(this,{status:state,expected:{monotonic:true,nominalDeltaUs:33333},actual:{...m,backwardJumps:backward,duplicatePts:duplicates,largeDiscontinuities:large},metrics:{samples:d.length},observations:['Tablet monotonic timestamps are retained only as per-device diagnostics.','Clock offset estimation: NOT IMPLEMENTED'],recommendation:state==='PASS'?null:'Inspect encoder PTS generation and discontinuities.'});}}];
